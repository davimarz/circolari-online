name: Robot Circolari ARGO

on:
  schedule:
    - cron: '0 8,16 * * *'  # 9:00 e 17:00 italiane
  workflow_dispatch:

jobs:
  scarica-circolari:
    runs-on: ubuntu-latest
    
    env:
      TZ: Europe/Rome
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Esegui scraper
      run: |
        echo "🤖 AVVIO SCRAPER ARGO"
        echo "====================="
        echo "🕐 Orario: $(date '+%H:%M:%S %Z')"
        echo "👤 User: davide.marziano.sc26953"
        echo "🔗 URL: https://www.portaleargo.it/voti/?classic"
        echo ""
        
        python leggi_circolari.py
        
        echo ""
        echo "✅ SCRAPER COMPLETATO"
        echo "🌐 App: https://circolari-online-production.up.railway.app"
