name: Robot Circolari

on:
  schedule:
    - cron: '0 6-23 * * *'   # Ogni ora dalle 6:00 alle 23:00 UTC
  workflow_dispatch:          # Esecuzione manuale

jobs:
  scarica-circolari:
    runs-on: ubuntu-latest
    
    env:
      TZ: Europe/Rome
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: pip install -r requirements.txt
    
    - name: Run robot
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}  # ⬅️ QUI USA IL SECRET
      run: |
        echo "🤖 Avvio robot..."
        echo "🇮🇹 Fuso orario: Italia"
        echo "🕐 Orario attuale: $(date '+%H:%M %Z')"
        python leggi_circolari.py
    
    - name: Notify success
      if: success()
      run: |
        echo "✅ Robot completato alle $(date '+%H:%M %Z')!"
        echo "🌐 App: https://circolari-online-production.up.railway.app"
